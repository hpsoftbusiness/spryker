env:
    NEW_RELIC_ENABLED: 0

stores:
    - MY
    - HK
    - AU
    - NZ
    - PH
    - TH
    - MO

sections:
    scheduler-stop:
        scheduler-clean:
            command: 'vendor/bin/console scheduler:suspend -vvv --no-ansi'
            stores: true
