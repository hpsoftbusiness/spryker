env:
    NEW_RELIC_ENABLED: 0

stores:
    - NO
    - FI
    - DK
    - SE

sections:
    scheduler-stop:
        scheduler-clean:
            command: 'vendor/bin/console scheduler:suspend -vvv --no-ansi'
            stores: true
