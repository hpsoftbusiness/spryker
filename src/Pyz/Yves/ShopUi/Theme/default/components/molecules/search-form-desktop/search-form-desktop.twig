{% extends model('component') %}

{% define config = {
    name: 'search-form-desktop'
} %}

{% define data = {
    searchValue: app.request.get('q')
} %}

{% define attributes = {
    'data-search-id': required
} %}

{% block body %}
    <div class="{{ config.name }}__input js-suggest-search__show">
        <input name="q" value="{{data.searchValue}}" type="text" class="{{ config.name }}__input__field {{ config.jsName }}__input--{{ attributes['data-search-id'] }}" placeholder="{{ 'global.search' | trans }}" autocomplete="off">

        {% include atom('icon') with {
            class: config.name ~ "__input__icon",
            modifiers: ['36'],
            data: {
                name: 'search',
            },
        } only %}
    </div>
{% endblock %}
