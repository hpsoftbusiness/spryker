{% extends template('widget') %}

{% define data = {
    product: product | default,
    products: products | default([]),
} %}

{% block body %}
    <div class="grid grid--stretch grid--with-gutter">
        {% for availableProduct in data.products if availableProduct.available %}
            {% widget 'CmsProductWidget' args [availableProduct] only %}{% endwidget %}
        {% endfor %}
    </div>

    {% if data.product is not empty %}
        <div class="grid grid--stretch grid--with-gutter">
            {% widget 'CmsProductWidget' args [data.product] only %}{% endwidget %}
        </div>
    {% endif %}
{% endblock %}
