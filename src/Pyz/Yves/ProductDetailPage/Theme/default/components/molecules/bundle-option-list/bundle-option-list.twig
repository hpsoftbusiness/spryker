{% extends model('component') %}

{% define config = {
    name: 'bundle-option-list',
    tag: 'ul'
} %}

{% define data = {
    bundleItems: required,
} %}

{% block class -%}
    {{ parent() }} grid
{%- endblock %}

{% block body %}
    {% for item in data.bundleItems %}
        <li class="col col--sm-12 {{ config.name }}__item">
            <span class="{{ config.name }}__quantity">{{ item.quantity }}x</span>
            {{ item.name }}
        </li>
    {% endfor %}
{% endblock %}
