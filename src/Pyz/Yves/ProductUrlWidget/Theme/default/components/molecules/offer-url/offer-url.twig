{% extends model('component') %}

{% define config = {
    name: 'offer-url',
} %}

{% block body %}
    {# TODO:: Style button #}
    <a style="float: right;position: relative;bottom: 50px; margin-right: 15px" class="button button--small"
       href="{{ data.url }} "
       target="{% if data.targetBlank %}_blank  {% endif %}">
        {{ 'page.detail.affiliate-product-action' | trans }}
    </a>
{% endblock %}
